import 'package:flutter/material.dart';
import 'dart:ui';

// Color ColorPrimary = new Color.fromRGBO(250, 252, 255, 0);
// Color ColorPrimary2 = new Color(0xFF4286f4); 0xFF -> the opacity (FF for opaque) 4286f4 -> the hex-color

const colorPrimary = Color(0XFF1c1e2d); // the dark blue of background
const colorPrimary_light = Color(0XFF25283b); // the light blue of card
const colorAccentBlue = Color(0XFF93e1eb); // the blue
const colorAccentGreen = Color(0XFF9fca5f); // the green
const colorAccentGray = Color(0XFF909197); // the gray
